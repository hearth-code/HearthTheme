---
import Layout from '../layouts/Layout.astro'
import ThemeForgeIsland from '../components/theme-forge/ThemeForgeIsland.astro'
import { t } from '../i18n/utils'

const lang = 'en'
---

<Layout lang={lang} title={t(lang, 'forge.meta.title')} description={t(lang, 'forge.meta.description')}>
  <ThemeForgeIsland lang={lang} />
</Layout>
