# Theme Review Sample

Use this file to check prose, links, inline `code`, and fenced blocks.

- Keyword lanes should stay visible.
- String lanes should not collapse into control colors.
- Links like [HearthCode](https://theme.hearthcode.dev) should remain calm.

```ts
const message = "signal stable"
if (message.includes("stable")) {
  console.log(message)
}
```
